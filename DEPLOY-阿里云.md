# 阿里云学生服务器部署（电脑之后可关机）

国内免费、能长期跑 WebSocket 的托管几乎没有。用你已有的**阿里云学生优惠**买一台轻量服务器最稳。

## 一、买服务器（约 5–10 分钟，手机也能操作）

1. 打开 https://www.aliyun.com  
2. 用你的账号登录（学生认证/优惠券在账号里）  
3. 搜索 **「轻量应用服务器」** 或进学生优惠页  
4. 配置建议：
   - 地域：离你们近的，如 **华东（杭州/上海）** 或 **华北**
   - 镜像：**Ubuntu 22.04 / 24.04**（或 Debian）
   - 带宽：3–5 Mbps 起（两人 PK 够用）
   - 时长：用学生优惠能买多久买多久
5. 支付完成后，记下 **公网 IP**，设置好 root 密码（或密钥）

> 价格以你账号里的学生券为准，常见大概一杯奶茶～几十元/月级别，比 Render 绑外卡省事。

## 二、安全组（必做）

阿里云控制台 → 该服务器 → **防火墙/安全组** → 添加规则：

- 协议 TCP，端口 `8765`，来源 `0.0.0.0/0`

不放行的话，手机打不开。

## 三、一条命令部署

用 SSH 登录服务器（阿里云控制台也可用「远程连接」）：

```bash
# 安装 git（若没有）
# Ubuntu: sudo apt update && sudo apt install -y git

git clone https://github.com/ltx200604/word-pk.git /opt/word-pk
bash /opt/word-pk/deploy/vps-deploy.sh
```

成功后会打印：

```
http://你的公网IP:8765
```

把这个网址发给对方，两人手机收藏。**之后你电脑不用开。**

## 四、日常

- 更新代码：服务器上 `cd /opt/word-pk && git pull && systemctl restart word-pk`
- 看日志：`journalctl -u word-pk -n 50`
- 重启：`systemctl restart word-pk`

## 五、和 Render 的差别

| | Render | 阿里云学生机 |
|--|--------|--------------|
| 支付 | 国际卡 | 支付宝/微信，学生券 |
| 数据 | 免费版易丢 | 磁盘在你机器上 |
| 维护 | 全托管 | 要自己 `git pull` 一下 |
| 费用 | 绑卡验证 | 用你的学生优惠 |

## 安全提醒

你之前发过的 GitHub / Render token，部署完后请去后台**撤销重建**。
